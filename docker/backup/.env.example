# Backup Service Environment Variables
# Copy this file to .env and configure

# Database Configuration (must match db service)
DB_USER=migestion
DB_PASSWORD=migestion123
DB_NAME=migestion

# Backup Settings
BACKUP_RETENTION_DAYS=30
BACKUP_SCHEDULE=0 2 * * *
